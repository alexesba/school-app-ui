import { dashboard } from '../paths';

export const add = `${dashboard}/level_programs/new`;
export const edit = `${dashboard}/level_programs/:id/edit`;
export const home = dashboard;
export const index = `${dashboard}/level_programs`;
export const show = `${dashboard}/level_programs/:id`;
