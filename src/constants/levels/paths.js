import { dashboard } from '../paths';

export const add = `${dashboard}/levels/new`;
export const edit = `${dashboard}/levels/:id/edit`;
export const home = dashboard;
export const index = `${dashboard}/levels`;
export const show = `${dashboard}/levels/:id`;
