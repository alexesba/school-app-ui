import { dashboard } from '../paths';

export const add = `${dashboard}/parents/new`;
export const edit = `${dashboard}/parents/:id/edit`;
export const home = dashboard;
export const index = `${dashboard}/parents`;
export const show = `${dashboard}/parents/:id`;
export const payments = `${dashboard}/payments`;
