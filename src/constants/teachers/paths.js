import { dashboard } from '../paths';

export const add = `${dashboard}/teachers/new`;
export const edit = `${dashboard}/teachers/:id/edit`;
export const home = dashboard;
export const index = `${dashboard}/teachers`;
export const show = `${dashboard}/teachers/:id`;
export const payments = `${dashboard}/payments`;
