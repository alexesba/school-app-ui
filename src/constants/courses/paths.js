import { dashboard } from '../paths';

export const add = `${dashboard}/courses/new`;
export const edit = `${dashboard}/courses/:id/edit`;
export const home = dashboard;
export const index = `${dashboard}/courses`;
export const show = `${dashboard}/courses/:id`;
