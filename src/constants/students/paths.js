import { dashboard } from '../paths';

export const add = 'students/new';
export const edit = 'students/:id/edit';
export const home = dashboard;
export const index = 'students';
export const show = 'students/:id';
