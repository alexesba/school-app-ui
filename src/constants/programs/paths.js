import { dashboard } from '../paths';

export const add = `${dashboard}/programs/new`;
export const edit = `${dashboard}/programs/:id/edit`;
export const home = dashboard;
export const index = `${dashboard}/programs`;
export const show = `${dashboard}/programs/:id`;
